name: Migration Contract Verification

on:
  pull_request:
    paths:
      - 'db/migrations/**'
      - 'db/schema/**'
      - '.github/workflows/migration-contract.yml'
  push:
    branches: [main]
    paths:
      - 'db/migrations/**'
      - 'db/schema/**'
  workflow_dispatch:

jobs:
  migration-verification:
    name: Verify Migration Integrity
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: union_eyes_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Apply migrations
        run: pnpm drizzle-kit push
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/union_eyes_test
      
      - name: Verify immutability triggers
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔒 IMMUTABILITY TRIGGER VERIFICATION"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          pnpm tsx scripts/verify-immutability-triggers.ts
          echo ""
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/union_eyes_test
      
      - name: Verify migration manifest integrity
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 MIGRATION MANIFEST VERIFICATION"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          pnpm tsx scripts/generate-migration-manifest.ts --output=migration-manifest-test.json --verify
          echo ""
          echo "✅ Migration manifest integrity verified"
          echo ""
      
      - name: Run migration contract tests
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🧪 MIGRATION CONTRACT TESTS (0062-0066)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          pnpm vitest run __tests__/db/migration-0062-0066.test.ts --reporter=verbose
          echo ""
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/union_eyes_test
      
      - name: Run RLS verification
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔍 RLS POLICY VERIFICATION"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          pnpm tsx scripts/scan-rls-usage-v2.ts --scope=tenant --max-violations=0
          echo ""
          echo "✅ RLS policies verified for critical tenant tables"
          echo ""
      
      - name: Generate verification report
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 MIGRATION VERIFICATION SUMMARY"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "✅ Migration application: SUCCESS"
          echo "✅ Immutability triggers: VERIFIED"
          echo "✅ Migration manifest: VERIFIED"
          echo "✅ Contract tests: PASSED"
          echo "✅ RLS policies: VERIFIED"
          echo ""
          echo "🎉 All migration contract requirements satisfied"
          echo ""
