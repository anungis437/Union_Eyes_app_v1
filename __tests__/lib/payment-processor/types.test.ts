/**
 * Tests for lib\payment-processor\types.ts
 * Auto-generated test skeleton - customize as needed
 */

import { describe, it, expect } from 'vitest';
import { PaymentProcessorError, PaymentIntentError, RefundError, WebhookVerificationError, CustomerError } from '@/lib/payment-processor/types';

describe('types', () => {
  it('exists', () => {
    expect(true).toBe(true);
  });

});
