/**
 * Database client export for packages/database
 * Re-exports the main database client from the root db directory
 */

export { db, client, checkDatabaseConnection, logDatabaseConnectionStatus } from '../../../db/db';
