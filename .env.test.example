# Test Redis Configuration
# This file sets up environment variables for testing the new Redis-based features

# Redis for Notification Worker (Development)
REDIS_HOST=localhost
REDIS_PORT=6379

# Upstash Redis for Distributed Caching (Development)
# For testing, you can use a local Redis instance or Upstash free tier
UPSTASH_REDIS_REST_URL=http://localhost:8079
UPSTASH_REDIS_REST_TOKEN=test-token

# Note: For actual testing, either:
# 1. Run local Redis: docker run -d -p 6379:6379 redis:alpine
# 2. Or temporarily disable Redis validation in tests
# 3. Or use Upstash free tier for testing
