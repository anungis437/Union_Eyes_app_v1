# External Calendar Sync Configuration
# Add these variables to your .env.local file

# ============================================================================
# GOOGLE CALENDAR API
# ============================================================================
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google Calendar API
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add authorized redirect URI: http://localhost:3000/api/calendar-sync/google/callback
# 6. Copy Client ID and Client Secret

GOOGLE_CALENDAR_CLIENT_ID=your_google_client_id_here
GOOGLE_CALENDAR_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/calendar-sync/google/callback

# ============================================================================
# MICROSOFT CALENDAR (OUTLOOK) API
# ============================================================================
# 1. Go to https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# 2. Click "New registration"
# 3. Enter name and select "Accounts in any organizational directory and personal Microsoft accounts"
# 4. Add redirect URI: http://localhost:3000/api/calendar-sync/microsoft/callback
# 5. Under "Certificates & secrets", create new client secret
# 6. Under "API permissions", add:
#    - Calendars.ReadWrite
#    - Calendars.ReadWrite.Shared
#    - offline_access
# 7. Copy Application (client) ID, Directory (tenant) ID, and Client Secret

MICROSOFT_CALENDAR_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CALENDAR_CLIENT_SECRET=your_microsoft_client_secret_here
MICROSOFT_TENANT_ID=common
MICROSOFT_CALENDAR_REDIRECT_URI=http://localhost:3000/api/calendar-sync/microsoft/callback

# ============================================================================
# PRODUCTION CONFIGURATION
# ============================================================================
# For production, update redirect URIs to:
# Google: https://yourdomain.com/api/calendar-sync/google/callback
# Microsoft: https://yourdomain.com/api/calendar-sync/microsoft/callback
