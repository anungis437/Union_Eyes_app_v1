{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", "*.js", "*.d.ts", "*.js.map", "*.d.ts.map"],
      "env": [
        "NEXT_PUBLIC_*",
        "NODE_ENV",
        "CLERK_*",
        "DATABASE_URL",
        "DIRECT_URL",
        "SENTRY_*"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "type-check": {
      "outputs": []
    }
  },
  "globalEnv": [
    "NODE_ENV"
  ],
  "globalDependencies": [
    "tsconfig.json",
    "next.config.mjs",
    "tailwind.config.ts",
    ".env",
    ".env.local",
    ".env.production"
  ]
}
